# 第三方组件与许可证（移动端发布包）

本仓库的移动端发布物**再分发**了下列第三方软件。它们**不是**本项目的作品，
版权归各自作者所有。这份文件是为了履行相应许可证的署名与源码提供义务。

---

## 1. Termux（`termux-app`）

| 项 | 内容 |
|---|---|
| 文件 | `termux-app_v0.118.3+github-debug_arm64-v8a.apk` |
| 版权 | Copyright (c) Termux contributors |
| 许可证 | **GPLv3 only**（另含例外，见下） |
| 上游仓库 | https://github.com/termux/termux-app |
| 精确来源 | Release 标签 [`v0.118.3`](https://github.com/termux/termux-app/releases/tag/v0.118.3) |
| 获取源码 | https://github.com/termux/termux-app/tree/v0.118.3 |
| 许可证全文 | https://github.com/termux/termux-app/blob/master/LICENSE.md |
| 本地副本 | 本仓库 `LICENSES/GPL-3.0.txt`（GPLv3 全文） |

**上游声明的例外**（摘自其 `LICENSE.md`，原文照录要点）：

- `Terminal Emulator for Android`（https://github.com/jackpal/Android-Terminal-Emulator）
  的代码以 **Apache 2.0** 发布，见其 `terminal-view` / `terminal-emulator` 两个库；
- `termux-shared` 库另有例外，见 https://github.com/termux/termux-app/blob/master/termux-shared/LICENSE.md

## 2. Termux:API（`termux-api`）

| 项 | 内容 |
|---|---|
| 文件 | `termux-api-app_v0.53.0+github.debug.apk` |
| 版权 | Copyright (c) Termux contributors |
| 许可证 | **GPLv3** |
| 上游仓库 | https://github.com/termux/termux-api |
| 精确来源 | Release 标签 [`v0.53.0`](https://github.com/termux/termux-api/releases/tag/v0.53.0) |
| 获取源码 | https://github.com/termux/termux-api/tree/v0.53.0 |
| 许可证声明 | https://github.com/termux/termux-api/blob/master/README.md（“License” 一节） |
| 本地副本 | 本仓库 `LICENSES/GPL-3.0.txt`（GPLv3 全文，与上同一个文件） |

---

## 3. 关于这两个 APK 的几个必须说清的点

1. **它们是 debug 构建，不是 F-Droid / Play 上的正式版。**
   上游 README 明确说明：除商店版本外，另提供**逐提交的 debug 构建**，
   文件名里的 `+github-debug` / `+github.debug` 就是指这个来源。
   上游正式分发渠道是 F-Droid（https://f-droid.org/packages/com.termux/ 与
   https://f-droid.org/packages/com.termux.api/）。

2. **两个 APK 必须同签名，否则 Termux:API 调不动。**
   上游说明：Termux:API 必须与主 Termux 用**同一个密钥**签名，
   否则 API 调用会被拒（只有主 Termux 允许调用这些 API）。
   本仓库分发的这两份来自**同一次上游构建**，证书 SHA-256 为：

   ```
   b6da0148eefd5fbf2cd3771b8d1021ec791304bdd6c4bf41d3faabad48ee5e1
   ```

   （此值来自发布包自带的说明文件；安装时若签名不一致，系统会拒绝覆盖安装。）

3. **GPLv3 的义务怎么履行的**：再分发 GPLv3 的二进制时，必须
   ①随附许可证全文、②提供对应源码的获取方式。
   本仓库用的是"**随附许可证副本 + 指向上游同标签源码**"的方式：

   - 许可证全文副本放在本仓库 `LICENSES/` 目录下；
   - 源码位置指向上游仓库的**对应 release 标签**（见上面两张表）。

4. **本项目的载荷包不是这些第三方组件的衍生作品。**
   `dsh-mobile-*.tar.gz` 是本项目自己的移动端封装（脚本 + 配置 + 安装器），
   与 Termux 通过公开的命令行接口协作；本仓库不包含也不需要 Termux 的源码改动。

---

## 4. 发布物清单与校验

每个发布物的 SHA256 由发布包里的 `manifest.json` 唯一定义
（发布脚本 `publish.py` 只读它，不在别处重写一遍文件名与哈希）。本次发布：

| 文件 | 去向 | SHA256（前 16 位） |
|---|---|---|
| `dsh-mobile-20260919.tar.gz` | 本仓库 `main` 分支 | `1299819c18a5d1da` |
| `bootstrap.sh` | 本仓库 `main` 分支 | `1882d9f639c7b81d` |
| `termux-app_v0.118.3+github-debug_arm64-v8a.apk` | Release `v20260919` | `72fdb596045116bf` |
| `termux-api-app_v0.53.0+github.debug.apk` | Release `v20260919` | `ecf916ff80ae751e` |

接收方的一键脚本 `bootstrap.sh` 会在下载后**逐条校验 SHA256**，
校验不通过就拒绝安装 —— 所以上面这些值不能事后改动。
