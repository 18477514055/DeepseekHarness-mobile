# 在电脑上把这个仓库建起来（最正规的分发方案）

## 这个文件夹里是什么
- `dsh-mobile-20260916.tar.gz` —— 安装包本体（153KB）
- `dsh-mobile-20260916.tar.gz.sha256` —— 校验和
- `dsh-mobile-一键安装.sh` —— 接收方要跑的一键脚本（已预填上面的 SHA256）

## 电脑上的步骤
1. GitHub 新建一个**公开**仓库，例如 `dsh-mobile`（私有仓库的 raw 链接需要 token，别用私有）。
2. 用网页「Add file → Upload files」把这 3 个文件传上去，Commit。
   - **不要**传 Termux 的 34MB APK：网页上传单文件限 25MB，超了会失败；
     需要的话用 Releases 传（Release 附件上限 2GB）。
3. 复制安装包的 raw 链接，形如：
   `https://raw.githubusercontent.com/<用户名>/<仓库名>/main/dsh-mobile-20260916.tar.gz`
4. 把这个链接回填给对方（手机上），或者直接告知手机端 DSH —— 它会把链接写进
   `dist/发布直链.txt` 并重打包，从此一键脚本**内置该地址 + 多源加速候选**。

## 验收（电脑上就能做）
```bash
curl -L -O https://raw.githubusercontent.com/<用户名>/<仓库名>/main/dsh-mobile-20260916.tar.gz
sha256sum dsh-mobile-20260916.tar.gz   # 应等于 .sha256 文件里那串
```
国内无 VPN 的接收方可以用加速前缀（任选其一，逐个试）：
```
https://cdn.jsdelivr.net/gh/<用户名>/<仓库名>@main/dsh-mobile-20260916.tar.gz
https://ghproxy.net/https://raw.githubusercontent.com/<用户名>/<仓库名>/main/dsh-mobile-20260916.tar.gz
https://gh-proxy.com/https://raw.githubusercontent.com/<用户名>/<仓库名>/main/dsh-mobile-20260916.tar.gz
https://ghfast.top/https://raw.githubusercontent.com/<用户名>/<仓库名>/main/dsh-mobile-20260916.tar.gz
```
